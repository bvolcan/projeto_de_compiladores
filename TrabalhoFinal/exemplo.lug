main{
    var int count;
    var int x;
    count := 0;
    x := 3;

    do {
        count := soma_um(count);
        print(count);
    } while((count < x));
}
function int soma_um(int x){
    return (x + 1);
}
